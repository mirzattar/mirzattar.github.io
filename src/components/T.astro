---
import { getPathLang, type PathWithLang, type TLanguageCodes } from "@i18n/ui";

const pathname = Astro.url.pathname as PathWithLang;
const pathLang: TLanguageCodes = getPathLang(pathname) ?? "id";
---

<slot name={pathLang} />
